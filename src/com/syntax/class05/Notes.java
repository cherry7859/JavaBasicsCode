package com.syntax.class05;

public class Notes {

	public static void main(String[] args) {
		
		/* Review
		 *if else if -> when need to test multiple conditions.
		 * 	Else if is nested within if
		 * 	if (false){
		 * 		code a;
		 *} 	else if {true}
		 *	c		ode b;
		 *Nested if -> when 1 condition depends on another condition
		 *	if (true) {
		 *  	code a;
		 * 		if (true) {
		 * 			code b; }}
		 * 
		 * Scanner -> used for user input
		 * import java.util.Scanner;
		 * Scanner obama = new Scanner(System.in);
		 */
		
		/* Logical Operators
		 * AND &&
		 * true && true -> TRUE
		 * true && false -> FALSE
		 * false && true -> FALSE
		 * false && false -> FALSE
		 * 
		 * OR ||
		 * true || true -> TRUE
		 * true || false -> TRUE
		 * false || true -> TRUE
		 * false || false -> FALSE
		 *
		 * NOT ! -> reverse the condition
		 * boolean checkboxSelected=true;
		 * if(!checkboxSelected) {
		 * click;}
		 */
	}
}
