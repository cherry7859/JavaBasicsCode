package com.syntax.class04;

public class Notes {
public static void main(String[] args) {
	
	/* If...else...if Statements
	 * When have multiple conditions to test
	 * 
	 * if (true condition) {
	 * code A;
	 * } else if (true condition) {
	 * code B;  
	 * } else if (true condition) {
	 * code C;
	 * } else {==when none of the conditions are true}
	 */
	
	/* Nested If
	 * if (boolean expression) {//outer if
	 * code A;
	 * if (boolean expression) {//nested/inner if
	 * code B;
	 * }
	 * }
	 * 
	 * Errors:
	 * Logical -> Code executes but does not produce expected results
	 * Syntax -> Code won't run
	 * 
	 * Null is used for non-primitive variable, means "nothing"
	 */
}
}
