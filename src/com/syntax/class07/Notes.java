package com.syntax.class07;
public class Notes {
public static void main(String[] args) {
	
	/*
	 * Loops: statement that repeats block of code "n" number of times
	 *	 Used to eliminate code redundancy
	 * 
	 * While Loop - repeats block of code based on the condition
	 * 		while(condition) {
	 * 			Repetitive code;
	 * 		} 
	 * 
	 * Do While Loop - Do executes, then the loop checks the condition
	 *		 do { 
	 * 			code
	 * 		} while (condition);
	 * 
	 * For loop - executes code as long as condition is true. Used when we know in advance how many times we want to repeat/iterate the code
	 * 		for (initialization; condition; increment/decrement) {
	 * 			block of code;
	 * 		}
	 * 
	 * Nested Loops - loop inside another loop
	 * outer loop must be true, to be able to execute nested loop
	 * 		
	 */

}
}
