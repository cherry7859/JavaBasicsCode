package com.syntax.class09;

public class Notes {

	/*
	 * Review
	 * break -> keyword that breaks the loop
	 * continue -> keyword that skips current execution/iteration
	 */
	
	/*
	 * Array --> container that can store multiple values(elements) to one variable
	 * 	stores based on indexes, start at 0
	 * int[] b
	 * 90; 	85; 	70; 100
	 * b[0]; b[1]; b[2]; b[3]
	 */
}
